interface RequestBodyProps {
  json: any; // JSON data as a string
  setRequestBody: any;
}
const RequestBody2 = ({ json, setRequestBody }: RequestBodyProps) => {
  return <div className="h-40 text-sm">Coming soon!!</div>;
};

export default RequestBody2;
